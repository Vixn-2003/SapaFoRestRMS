@model WebSapaForestForStaff.DTOs.UserManagement.CreateStaffRequest

@{
    ViewData["Title"] = "Tạo nhân viên mới";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container-fluid">
    <div class="page-header">
        <div class="header-content">
            <h1 class="page-title">Tạo nhân viên mới</h1>
            <p class="page-subtitle">Chỉ cần họ tên và email – nhân viên sẽ hoàn thiện hồ sơ sau</p>
        </div>
        <div class="page-actions">
            <a asp-action="Index" class="btn-secondary-custom btn-medium">
                <i class="mdi mdi-arrow-left"></i> Quay lại
            </a>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-7">
            <div class="card-custom">
                <div class="card-header-custom d-flex justify-content-between align-items-center">
                    <h5 class="card-title mb-0 heading-h2">
                        <i class="mdi mdi-account-plus"></i> Thông tin tối thiểu
                    </h5>
                </div>
                <div class="card-body-custom">
                    <form asp-action="CreateStaff" method="post" id="createStaffForm" data-send-code-url="@Url.Action("SendStaffVerificationCode", "Users")">
                        @Html.AntiForgeryToken()

                        <div class="mb-3">
                            <label asp-for="FullName" class="form-label-custom required"></label>
                            <input asp-for="FullName" class="form-control-custom" placeholder="Nhập họ và tên đầy đủ" />
                            <span asp-validation-for="FullName" class="text-danger body-small"></span>
                        </div>

                        <div class="mb-3">
                            <div class="d-flex justify-content-between align-items-center">
                                <label asp-for="Email" class="form-label-custom required mb-0"></label>
                                <button type="button" class="btn-secondary-custom btn-small" id="sendVerificationBtn">
                                    <i class="mdi mdi-email-send-outline"></i> Gửi mã xác minh
                                </button>
                            </div>
                            <input asp-for="Email" type="email" class="form-control-custom" placeholder="example@email.com" />
                            <small class="body-small text-gray-custom">Mã OTP sẽ được gửi tới email này. Nhân viên cần đọc email và cung cấp mã cho bạn.</small>
                            <span asp-validation-for="Email" class="text-danger body-small"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="VerificationCode" class="form-label-custom required"></label>
                            <input asp-for="VerificationCode" class="form-control-custom" maxlength="6" placeholder="Nhập mã 6 chữ số" />
                            <span asp-validation-for="VerificationCode" class="text-danger body-small"></span>
                        </div>

                        <div class="d-flex justify-content-end gap-2 mt-4">
                            <a asp-action="Index" class="btn-secondary-custom btn-medium">
                                <i class="mdi mdi-close"></i> Hủy
                            </a>
                            <button type="submit" class="btn-primary-custom btn-medium" id="submitCreateStaff">
                                <i class="mdi mdi-check-circle"></i> Tạo nhân viên
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-lg-5">
            <div class="card-custom">
                <div class="card-header-custom">
                    <h5 class="card-title mb-0 heading-h2">
                        <i class="mdi mdi-shield-check-outline"></i> Quy trình xác minh
                    </h5>
                </div>
                <div class="card-body-custom">
                    <ol class="body-base mb-3">
                        <li>Nhập họ tên và email của nhân viên.</li>
                        <li>Bấm <strong>Gửi mã xác minh</strong> để hệ thống gửi OTP tới email đó.</li>
                        <li>Nhân viên đọc email và cung cấp mã OTP.</li>
                        <li>Nhập mã OTP và bấm <strong>Tạo nhân viên</strong>.</li>
                    </ol>
                    <div class="alert alert-info body-small" style="background-color: var(--primary-100); border-color: var(--primary-300); color: var(--primary-700);">
                        <i class="mdi mdi-information-outline"></i>
                        Nhân viên có thể bổ sung số điện thoại và thông tin cá nhân sau khi đăng nhập lần đầu.
                    </div>
                    <div class="mt-3">
                        <h6 class="heading-h2 mb-2">Lợi ích:</h6>
                        <ul class="body-base mb-0">
                            <li>Đảm bảo email thuộc về nhân viên trước khi tạo tài khoản.</li>
                            <li>Hạn chế lỗi nhập sai email hoặc spam tài khoản.</li>
                            <li>Tự động phát sinh mật khẩu tạm thời gửi cho quản lý.</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Styles {
    <link rel="stylesheet" href="~/css/users.css">
    <link rel="stylesheet" href="~/css/users/createStaff.css">
}

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script src="~/js/toast-notification.js"></script>
    <script src="~/js/users/createStaff.js"></script>
}

